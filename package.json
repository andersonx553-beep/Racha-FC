{
  "name": "racha-do-graxinha",
  "version": "1.0.0",
  "private": true,
  "description": "Racha do Graxinha - app web empacotado com Capacitor para Android",
  "scripts": {
    "web:prepare": "node scripts/prepare-web.mjs",
    "cap:add:android": "npm run web:prepare && npx cap add android",
    "cap:sync": "npm run web:prepare && npx cap sync android",
    "cap:open": "npm run cap:sync && npx cap open android",
    "cap:copy": "npm run web:prepare && npx cap copy android"
  },
  "dependencies": {
    "@capacitor/android": "8.5.2",
    "@capacitor/core": "8.5.2",
    "@capacitor-firebase/authentication": "8.5.2",
    "firebase": "12.19.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.2",
    "@capacitor/assets": "3.0.5"
  },
  "engines": {
    "node": ">=22"
  }
}
